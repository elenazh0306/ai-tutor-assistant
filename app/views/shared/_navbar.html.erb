<nav class="navbar navbar-expand-sm navbar-light">
  <div class="container-fluid">

    <!-- Commenting out the image, but keeping the code in case we want it later
    <%= link_to subjects_path, class: "navbar-brand" do %>
      <%= image_tag "wizard_tutor_portrait.jpg" %>
    <% end %>
    -->

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <!-- commenting this <li>, but keeping the code if we want it later
            <li class="nav-item active">
              <%= link_to "Back to Subjects", subjects_path, class: "nav-link" %>
            </li>
            -->
            <li class="nav-item active">
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link button-new" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "nav-link button-new" %>
            </li>
          <% end %>
        </ul>
    </div>
  </div>
</nav>
