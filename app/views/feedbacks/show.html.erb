<div class = "container py-4 feedback-page">
  <h1><%= @test.title %> Feedback</h1>

<!--
<% @questions.each_with_index do |question, index| %>
  <p>Question <%= index + 1 %></p>
  <p><%= question.title %></p>
  <p><%= question.answer %></p>
<% end %>
-->

<%= simple_format(@feedback.content) %>

<%= link_to "← Return to Subject", subject_path(@subject), class: "text-decoration-none text-secondary fs-4" %>
</div>
