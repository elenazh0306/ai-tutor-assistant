<h1>Review and save these Materials</h1>
<%= simple_form_for [@subject, @material] do |f| %>
  <%= f.input :title %>
  <%= f.input :content, as: :text, input_html: { rows: 8 }%>
  <%= f.submit %>
<% end %>

<%= @chat.id %>

<%= link_to "Cancel Edit", subject_path(@subject) %>
