<div> <!-- possible hero banner -->
  <h1><%= @material.name %></h1>
</div>

<div>
  <!-- Study material text goes here -->
</div>

<div> <!-- possible PDF-creator here -->
  <button>export to PDF</button>
</div>

<div> <!-- style this as a nice dashboard? -->
  <%= link_to "Back to Subject", subject_path(@subject) %>
  <%= link_to "Edit these Study Materials", edit_subject_material_path(@material) %>
  <!-- we pass both @subject and @material to the link helper because the path is /subjects/:subject_id/materials/:id -->
  <%= link_to "Delete This Material", subject_material_path(@subject, @material), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this set of study materials?"} %>
</div>
